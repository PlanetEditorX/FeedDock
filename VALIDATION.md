# FeedDock 1.17.6 验证报告

## 功能验证

- 默认剧集目录为 `名称 (年份)/Season 01`；
- 默认电影目录为 `名称 (年份)`；
- 有 TMDB ID 时不再附加 `[tmdbid=...]`；
- TMDB ID 仍保留在命名上下文、数据库和 NFO `uniqueid` 中；
- 用户显式配置的自定义模板仍可使用 `{tmdb_id}`；
- 下载路径预览、新订阅保存和本地刮削均使用相同目录规则；
- 已存在目录不会自动重命名，避免破坏 qBittorrent 做种路径和媒体库索引。

## 自动化与静态验证

- 151 项自动化测试通过；
- Python 全项目编译通过；
- 6 个 JavaScript 文件语法检查通过；
- Docker Compose、飞牛 Compose 和 GitHub Actions YAML 解析通过；
- 页面 HTML ID 唯一性检查通过；
- FastAPI 导入与运行版本 `1.17.6` 检查通过。

## 数据兼容

不增加数据库字段，不执行目录重命名迁移。升级前已经创建的 qBittorrent 任务和媒体目录继续使用原路径；升级后的新任务使用 `名称 (年份)`。
