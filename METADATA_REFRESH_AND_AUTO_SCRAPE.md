# FeedDock 1.17.4 元数据刷新与下载完成后自动刮削

## 刷新菜单

顶部“刷新”菜单现在包含两个互不影响的动作：

- **刷新全部订阅**：读取所有启用订阅的 RSS，执行匹配、去重并推送下载器；点击后直接执行，不显示二次确认。
- **同步订阅元数据**：不读取 RSS，也不创建下载任务；只更新订阅的标题、评分、海报、总集数和关联 ID。

下拉菜单整体向下偏移，并在点击任一操作后立即收起，避免遮挡顶部导航。

## 下载完成后自动刮削

新安装和恢复默认设置时，“下载完成后自动刮削”默认开启。FeedDock 中的“刮削”表示同步外部元数据，不会下载 NFO 或海报文件。

下载完成检查会按以下顺序处理：

1. 确认 qBittorrent 任务已经完成；
2. 按元数据同步周期判断订阅数据是否需要更新；
3. 使用已有 TMDB、Bangumi 或 AniList ID，同步标题、评分、海报和总集数；
4. 如启用 `bangumi.ini`，在剧集目录的上级剧集目录中生成该文件；
5. 将条目的刮削状态记录为完成或错误。

元数据同步失败不会把已下载任务改回下载失败。下载状态仍保持完成，只有刮削状态显示错误，并可在“下载”页面点击“重试处理”。

## 批量同步日志

“同步订阅元数据”会写入：

- 开始同步订阅元数据；
- 每个订阅的成功来源和元数据 ID；
- 每个失败订阅的异常详情；
- 总订阅数、成功数和错误数。

日志同时显示在网页日志页，并写入 `/data/logs/feeddock.log`。

## 升级行为

本版本不增加数据库字段。已经明确保存为关闭的自动刮削设置会继续保持关闭；没有保存过该设置的新安装或恢复默认配置会默认开启。
