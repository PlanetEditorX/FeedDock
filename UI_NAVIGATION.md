# FeedDock 1.17.8 界面与导航说明

## 设计目标

FeedDock 1.13.0 起将首页从“所有配置面板纵向堆叠”改为“订阅优先的任务式控制台”。登录后首先看到订阅统计和订阅列表，添加、下载、刷新、管理、设置和日志通过顶部导航进入独立视图。

设计原则：

1. 高频、低风险操作靠前：添加、查看下载、刷新订阅；
2. 批量操作集中到管理，不混入单个订阅编辑器；
3. 低频配置按职责拆分，不再挤占首页；
4. 退出、重启、关闭和更新归入系统管理，并将破坏性操作放在最后；
5. 保留 FeedDock 既有能力，不为了模仿界面而复制 ani-rss 的技术结构。

## 顶部导航顺序

### 添加

#### 订阅站点入口

- **Mikan**：读取 Mikan 原站季度目录和字幕组；
- **ANI.BT**：读取 ANI.BT 原站季度周历、字幕组和专用 RSS；
- **Anime Garden（AG）**：读取 Anime Garden 原站当前活跃番剧、字幕组和专用 RSS；
- **其它 RSS**：添加任意标准 RSS、Atom 或 RDF 地址；
- **添加合集**：批量导入一组订阅定义。

站点定义由 `/api/subscription-sources` 提供。订阅列表的来源标签由后端基于真实 URL 主机名计算，未知站点仍优先显示用户填写的 RSS 名称。详细说明见 [`SUBSCRIPTION_SOURCES.md`](SUBSCRIPTION_SOURCES.md)。


### 下载

显示最近发现和推送的条目，包括：

- 等待定时推送；
- 已推送；
- 已跳过；
- 错误；
- qBittorrent 下载进度；
- 规范命名结果。

页面保留“检查下载完成”和“清理条目”。清理只删除页面历史记录，RSS 去重指纹仍然保留。

### 刷新

刷新菜单包含“刷新全部订阅”“同步订阅元数据”和“刮削已完成媒体”。前者读取 RSS，中间项只更新 FeedDock 数据库，最后一项向历史已完成媒体写入 NFO 与图片。

订阅列表中每张卡片另有“刮削”按钮，只处理该订阅已经完成的下载条目。该入口适合修复单个订阅的路径配置或补写 NFO，无需再次扫描全部历史媒体。

### 管理

- **查看订阅状态**：回到订阅首页；
- **批量管理**：进入订阅首页并开启选择模式；
- **导入订阅**：导入 FeedDock JSON；
- **导出订阅**：导出全部订阅。

批量模式支持：

- 全选当前搜索与状态筛选结果；
- 批量启动；
- 批量禁用；
- 批量删除；
- 导出所选；
- 导入订阅。

批量删除要求二次确认。搜索和状态筛选只影响当前展示与“全选当前结果”，不会修改未显示的订阅。

### 设置

设置按职责拆成九个独立视图，并按“日常外观 → 媒体处理 → 下载/RSS → 网络与安全 → 系统”排列：

1. **页面设置**：主题色，以及按评分、拼音或更新时间排列订阅；
2. **刮削设置**：下载完成后 NFO/图片写入、元数据同步、追更天数、TMDB 地址/密钥和 `bangumi.ini`；
3. **下载设置**：qBittorrent 连接、失败重试、并发限制和做种时长；
4. **RSS 设置**：总开关、轮询间隔、超时、文件跳过和完结自动停用；
5. **Trackers**：Tracker 更新地址、缓存状态和手动刷新；
6. **代理设置**：外部请求代理及不使用代理列表；
7. **登录设置**：查看当前账号、修改密码和退出登录；
8. **通知**：Telegram、Bark、Webhook 和事件开关；
9. **系统管理**：退出登录、重启、关闭、检查更新和执行更新。

页面、刮削、下载、RSS 和 Tracker 的详细依赖关系见 [`SETTINGS_REFERENCE.md`](SETTINGS_REFERENCE.md)。

重启和关闭默认不可用。只有显式设置以下环境变量后，后端才接受对应请求：

```dotenv
FEEDDOCK_ALLOW_SYSTEM_ACTIONS=true
```

重启操作会让 FeedDock 进程以状态码 `75` 退出，是否自动恢复取决于 Docker/Compose 的重启策略。关闭操作发送 `SIGTERM`；如果容器配置为始终重启，容器运行时仍可能再次拉起服务。

### 日志

日志视图集中显示：

- 当前日志级别；
- 日志文件路径；
- INFO/DEBUG 运行记录；
- 手动刷新和清理操作。

日志放在导航末端，便于故障排查，同时避免干扰日常订阅操作。

## 订阅首页

首页仅展示：

- 启用订阅、已推送、已跳过和错误统计；
- 订阅搜索；
- 启用、停用和异常状态筛选；
- 订阅卡片；
- 可选的批量管理工具栏。

每个订阅卡片保留单独编辑、启用/停用、立即检查和删除能力。五个周历站点都会在当前站点目录中即时显示“已订阅”标识。

## URL 与浏览器行为

界面使用 URL Hash 标识当前视图，例如：

```text
/#subscriptions
/#downloads
/#settings-download
/#settings-notification
```

因此刷新浏览器后仍能回到当前视图，也可以直接收藏具体设置页面。未知 Hash 会安全回退到订阅首页。

桌面端显示横向顶部导航；窄屏设备会自动换行并把弹出菜单限制在可视宽度内。

## 导入与导出格式

导出文件结构：

```json
{
  "format": "feeddock-subscriptions",
  "version": 1,
  "app_version": "1.17.8",
  "subscriptions": [
    {
      "name": "示例番剧",
      "rss_url": "https://example.test/feed.xml",
      "enabled": true
    }
  ]
}
```

实际订阅对象还会包含备用 RSS、匹配规则、元数据、命名、完结停用和停更检测等可创建字段，但不会导出数据库 ID、下载历史、通知去重状态或 RSS 指纹。

导入冲突以主 `rss_url` 判断：

- **跳过**：保留数据库中的现有订阅；
- **更新**：用导入内容覆盖该订阅的可配置字段。

单次最多导入 500 条订阅；批量操作单次最多处理 1000 个 ID。

## “添加合集”的边界

当前“合集”表示“一组可一次导入的订阅定义”，不是数据库中的永久文件夹或标签。

这样设计的原因：

- 可直接用于备份、迁移和分享配置；
- 不改变现有订阅、条目和调度数据模型；
- 避免出现分组删除是否连带删除订阅等歧义；
- 后续如果加入永久分组，可在明确排序、权限和继承规则后单独迁移。

## 升级说明

升级到 1.17.0 会自动增加订阅来源、站点番剧 ID、统一身份字段和跨站隐藏偏好表；既有订阅、条目与缓存保持不变，无需手工 SQL。

静态资源使用 `v=1.17.8` 缓存参数。升级后建议强制刷新一次浏览器，特别是反向代理仍缓存旧 HTML 时。

## 刷新菜单

- 刷新全部订阅：直接启动 RSS 刷新，不显示二次确认。
- 同步订阅元数据：更新全部订阅的外部元数据，不读取 RSS。
- 菜单向下偏移，并在点击操作后自动收起。
