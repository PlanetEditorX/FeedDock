# FeedDock 1.17.6 设置说明

FeedDock 1.17.6 延续现有设置体系，并将页面、真实本地刮削、下载、RSS 和 Tracker 策略接入 FastAPI、SQLite 与 qBittorrent 流程。网页保存的设置位于 SQLite `app_settings` 表，并优先于 Compose 默认值。

## 页面设置

### 主题色

提供海蓝、靛青、青绿、橙色和玫红五种主题。主题选择会同时保存到服务器和浏览器 `localStorage`，因此页面重新加载前即可应用，减少主题闪烁。

### 订阅排序

- **评分**：按已同步的 TMDB、Bangumi 或 AniList 评分从高到低排列；无评分项目随后按拼音排列。
- **拼音**：使用浏览器中文拼音排序规则排列订阅名称。
- **更新时间**：按订阅更新时间从新到旧排列。

评分只在元数据同步成功后更新，不会凭 RSS 标题自行推测。

## 刮削设置

FeedDock 的“自动刮削”包含两步：同步外部元数据，并将标准 NFO、海报与背景图写入统一媒体根目录。FeedDock 不直接修改媒体服务器数据库。

### 自动刮削

开启后，下载完成检查会同步到期的外部元数据，并向实际媒体目录写入 NFO 与图片。对最近仍在追更的订阅，RSS 检查仍会按追更窗口定期同步元数据。

### 追更天数

默认 `14` 天。全局自动刮削只处理最近一次发现新匹配条目、检查或创建时间仍在该窗口内的订阅，避免长期完结订阅持续请求元数据服务。

### bangumi.ini

开启后，在下载完成时于番剧根目录写入：

```ini
[Bangumi]
id=123456
```

电视番剧保存到季度目录时，文件写在 `Season 01` 的上一级番剧目录；电影则写在下载目录本身。写入路径必须位于统一媒体根目录内，并采用临时文件加原子替换。

开启此功能时，历史上已经完成但此前跳过侧车文件的任务会进入一次性补写队列。没有 Bangumi ID 的订阅会安全跳过。

### TMDB 地址和密钥

默认值：

```text
TMDB API:   https://api.themoviedb.org
TMDB Image: https://image.tmdb.org
```

FeedDock 内部会补全 `/3` 和 `/t/p` 路径，也兼容用户直接填写已带这些路径的地址。

认证字段同时支持：

- 32 位 TMDB v3 API Key；
- TMDB v4 Read Access Token。

网页留空会保留已经保存的值；未保存网页值时使用 Compose 中的 `TMDB_READ_ACCESS_TOKEN`。密钥不会通过普通设置接口返回明文。

## 下载设置

### 失败重试次数

默认 `2`，表示首次推送失败后最多再重试两次，共尝试三次。重试只针对向 qBittorrent 创建任务的操作，不会重复创建已经成功提交的任务。

### 同时下载限制

默认 `3`；`0` 表示不限。FeedDock读取指定 qBittorrent 分类下的活动下载数，达到限制时把新条目标记为等待并发空位。后台每两分钟重新尝试这些条目。

并发等待、成功推送、跳过和错误分别统计，等待空位不会再被误报为已推送。

### 做种时长

单位为分钟：

- `-1`：使用 qBittorrent 默认值；
- `0`：下载完成后停止做种；
- 正整数：把该任务的做种时间限制传给 qBittorrent。

最终行为仍受 qBittorrent 版本、队列和分享率规则影响。

## RSS 设置

### RSS 开关

关闭后，后台轮询、手动刷新全部和单订阅处理都不会读取 RSS。下载完成检查仍会继续运行，以便已推送任务完成命名、通知和侧车文件处理。

### RSS 间隔与超时

- 默认间隔：`30` 分钟；允许 `5–1440` 分钟。
- 默认超时：`20` 秒；允许 `5–300` 秒。

RSS 超时同时用于自动元数据同步、qBittorrent 调用和 Tracker 更新等与该任务流程相关的网络操作。

### 文件已下载自动跳过

该选项依赖规范文件名，只在以下条件全部满足时生效：

1. 订阅已开启自动重命名；
2. FeedDock 与 qBittorrent 将同一宿主机目录映射为相同容器路径；
3. 目标目录内存在与预期规范文件名同名的视频文件。

FeedDock 只在计算出的目标目录直接检查最多 2000 个文件，并进行不区分大小写的完整文件干名匹配，不递归扫描整个媒体库。

开启全局自动跳过后，服务端会阻止以下无效操作：

- 新建一个启用但关闭自动重命名的订阅；
- 把启用订阅改为关闭自动重命名；
- 导入不符合条件的启用订阅；
- 批量启用不符合条件的订阅。

### 自动禁用订阅

全局开启后，具有 Bangumi ID 且未锁定总集数的订阅每 12 小时最多刷新一次总集数。只有第 `1..总集数` 的整数集都被 qBittorrent 确认为下载完成时，订阅才会自动停用。

`.5` 特别篇不会用于满足整季完成条件。单个订阅原有的“全部下载完成后自动停用”开关仍可独立启用。

## Trackers

默认更新地址：

```text
https://cf.trackerslist.com/best.txt
```

点击“立即更新”后，FeedDock：

1. 通过统一代理策略下载文本；
2. 过滤无效协议并去重；
3. 最多缓存 500 个 Tracker；
4. 在 qBittorrent 返回新任务哈希后追加到该任务。

Tracker 缓存存入 SQLite。功能不会删除任务已有 Tracker，也不会因为 Tracker 更新失败而把下载任务标记为失败。更换更新地址后需要手动点击一次“立即更新”来刷新缓存。

## 数据库与升级

1.14.0 启动时增量增加：

### `subscriptions`

- `metadata_rating`
- `total_episodes_checked_at`

### `feed_items`

- `trackers_status`
- `trackers_message`
- `trackers_applied_at`

页面、下载、RSS、Tracker 和刮削策略本身保存在现有 `app_settings` 表。迁移只新增列，不删除订阅、下载历史、RSS 指纹或通知配置。

## 安全边界

- TMDB 密钥、qBittorrent 密码和通知密钥不会由普通设置接口返回明文。
- 自动跳过只读取统一媒体根目录内的目标目录。
- `bangumi.ini` 只能写入统一媒体根目录内部。
- Tracker 与侧车文件处理失败不会撤销已成功创建的下载任务。
- FeedDock 不自动删除媒体文件、旧版本或正在做种的文件。

## 下载完成后自动刮削

该选项默认开启。qBittorrent 下载完成后，FeedDock 会同步标题、简介、评分、海报地址和总集数，并写入 `tvshow.nfo`/`movie.nfo`、季 NFO、剧集同名 NFO、海报与背景图。失败仅影响刮削状态，不改变下载完成状态。历史完成任务可通过“刷新 → 刮削已完成媒体”补写；`bangumi.ini` 仍由独立开关控制。
