# 可选：使用 GitHub Actions 发布的预构建镜像
# FEEDDOCK_IMAGE=ghcr.io/planeteditorx/feeddock:latest

# Web 管理界面
APP_PORT=7789
APP_NAME=FeedDock
FEEDDOCK_BUILD_VERSION=1.5.0
ADMIN_USER=admin
# 仅作为首次登录密码；首次成功登录后系统会强制修改并保存到 data/feeddock.db
ADMIN_PASSWORD=change-this-to-a-strong-password
SESSION_DAYS=7
# 通过 HTTPS 访问时设为 true
COOKIE_SECURE=false
TZ=Asia/Shanghai

# RSS 调度
POLL_INTERVAL_MINUTES=10
REQUEST_TIMEOUT_SECONDS=20
RSS_USER_AGENT=FeedDock/1.5 (+self-hosted RSS automation)

# 搜索来源。Mikan 会依次尝试主地址和备用地址；动漫花园使用关键词 RSS。
MIKAN_BASE_URL=https://mikanime.tv
MIKAN_FALLBACK_URLS=https://mikanani.me,https://mikanani.kas.pub
DMHY_BASE_URL=https://share.dmhy.org

# qBittorrent Web API
# 使用外部 qBittorrent 时直接填写局域网或公网 HTTPS 地址，不需要启动 with-qbit profile
QBIT_URL=http://192.168.1.20:8080
QBIT_USERNAME=admin
QBIT_PASSWORD=替换为qBittorrent密码
QBIT_CATEGORY=rss

# FeedDock 发给 qBittorrent 的路径，必须是 qBittorrent 所在主机/容器可识别的路径
DOWNLOAD_PATH=/downloads/rss

# 可选：检查 GitHub Release
UPDATE_REPOSITORY=planeteditorx/feeddock
UPDATE_API_URL=https://api.github.com
# 可选：GitHub Token 可提高 API 限额；留空时仍可手动检查公开 Release
UPDATE_GITHUB_TOKEN=

# 可选：网页一键更新。启用 updater profile 时必须设置随机长令牌
WATCHTOWER_URL=http://watchtower:8080
WATCHTOWER_TOKEN=替换为至少32位随机字符串

# 可选内置 qBittorrent 容器
PUID=1000
PGID=1000
QBIT_WEBUI_PORT=8080
