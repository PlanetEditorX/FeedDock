# 当前飞牛 OS 默认 Compose 已直接写入基础配置，一般不需要创建 .env 文件。
# 首次登录账号：admin
# 首次登录密码：password
# 登录后系统会强制要求设置新密码。
# 新密码保存在 /vol1/1000/应用/feeddock/data 中，不会被 Compose 里的初始密码覆盖。
# Mikan 已浏览季度默认每 6 小时后台刷新；封面使用本地优先缩略图缓存。
# 浏览器默认缓存封面 30 天；服务端本地图片有效时不会重新访问 Mikan。
MIKAN_IMAGE_CACHE_DAYS=30
MIKAN_THUMBNAIL_WIDTH=240
MIKAN_THUMBNAIL_HEIGHT=320

# 元数据、Emby 规范命名与可选本地刮削
METADATA_LANGUAGE=zh-CN
METADATA_AUTO_SYNC_HOURS=24
TMDB_API_BASE=https://api.themoviedb.org
TMDB_IMAGE_BASE=https://image.tmdb.org
# 支持 32 位 v3 API Key 或 v4 Read Access Token
TMDB_READ_ACCESS_TOKEN=
BANGUMI_ACCESS_TOKEN=
# 仅在把媒体目录挂载给 FeedDock 时填写，例如 /media
MEDIA_LOCAL_ROOT=
EMBY_URL=
EMBY_API_KEY=

# 飞牛默认以 root 运行，避免宿主机媒体目录因 UID 1000 无法写入。
PUID=0
PGID=0
UMASK=002
DOWNLOAD_PATH=/media
MEDIA_LOCAL_ROOT=/media

ANILIST_API_URL=https://graphql.anilist.co
TMM_URL=
TMM_API_KEY=
AUTOMATION_TIME=02:00
AUTOMATION_TIMEZONE=Asia/Shanghai
OUTBOUND_PROXY_URL=
OUTBOUND_NO_PROXY=localhost,127.0.0.1,host.docker.internal

# 日志级别：INFO 或 DEBUG
LOG_LEVEL=INFO

# 允许网页触发重启/关闭；默认关闭
FEEDDOCK_ALLOW_SYSTEM_ACTIONS=false
