# FeedDock 1.17.11 RSS 快速更新

## 使用方式

订阅卡片新增“更新 RSS”。点击后会直接打开编辑页面的 RSS 区域，并选中主 RSS 地址。

编辑状态下有两个独立按钮：

- **仅保存 RSS**：只更新主/备用 RSS 名称与地址，不修改其它订阅设置；
- **保存 RSS 并立即检查**：保存后只刷新当前订阅，不刷新全部订阅。

RSS 地址发生变化后，后端会重新识别 Mikan、ANI.BT、Anime Garden 或其它 RSS，并重新提取原站番剧 ID。

## 安全行为

- 使用浏览器 URL 校验；
- 仍受后端 HTTP/HTTPS URL 校验约束；
- 停用订阅可以保存 RSS，但立即检查会记录“已停用，未执行刷新”；
- 检查日志会显示“订阅刷新开始/完成”和触发来源 `rss-updated`。
