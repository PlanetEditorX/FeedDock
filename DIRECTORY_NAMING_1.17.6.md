# FeedDock 1.17.6 默认目录命名

默认电影和剧集目录现在统一采用：

```text
名称 (年份)
```

电视剧示例：

```text
金牌得主 (2025)/Season 02
```

电影示例：

```text
电影名称 (2026)
```

TMDB ID 不再附加为 `[tmdbid=123]`。它仍保存在 FeedDock 数据库和 NFO 的 `uniqueid` 字段中，因此不会影响媒体识别。

为了保护 qBittorrent 做种路径和现有媒体库索引，升级不会自动重命名已经存在的目录。新任务和重新计算保存路径的任务会使用新格式。用户自定义模板仍可显式使用 `{tmdb_id}`。
