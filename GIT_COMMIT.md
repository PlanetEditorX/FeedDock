# FeedDock 1.17.6 Git 提交说明

## 提交标题

```text
fix(naming): 默认媒体目录仅保留名称与年份
```

## 提交正文

```text
- 默认电影与剧集目录改为“名称 (年份)”
- 去除默认目录中的 [tmdbid=...] 后缀
- TMDB ID 继续写入数据库和 NFO uniqueid
- 保留自定义模板中的 {tmdb_id} 变量
- 不自动重命名已有目录，避免影响做种和媒体库索引
- 更新保存路径预览、刮削测试和使用说明
```

## 数据库

不增加数据库列，也不需要数据迁移。已有目录和已创建任务保持原路径；新任务使用新的默认目录格式。
