# FeedDock 1.17.5 Git 提交说明

## 提交标题

```text
feat(scraper): 写入媒体库 NFO 与图片
```

## 提交正文

```text
- 修正自动刮削仅同步数据库、不写媒体目录的问题
- 下载完成后生成电视、季度、剧集和电影 NFO
- 下载并复用 poster、fanart 与季度海报
- 增加媒体根目录越界保护和原子文件写入
- 增加“刮削已完成媒体”历史补写入口
- 将旧版已完成条目一次性加入本地刮削补写队列
- 记录媒体目录、生成文件和失败原因
```

## 数据库

不增加数据库列。启动时写入一次性迁移标记：

```text
migration:1.17.5:local-scrape-backfill
```

该迁移会把已有的已完成条目标记为等待写入 NFO 与图片。
